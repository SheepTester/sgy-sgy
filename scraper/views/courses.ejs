<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>courses.</title>
  </head>
  <body>
    <h1>courses.</h1>
    <ul>
      <% for (const { id, title } of courses) { %>
        <li><a href="/courses/<%= id %>/"><%= title %></a></li>
      <% } %>
    </ul>
  </body>
</html>
